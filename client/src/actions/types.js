//in this file we are going to list all the dispatch calls that are going to be triggered by the action

export const SET_CURRENT_USER = "SET_CURRENT_USER";
//"SET_CURRENT_USER" is the diaptch call ..we are storing it in a var because when we call the string everywhere there are chances of geting typo error.

export const GET_ERRORS = "GET_ERRORS";